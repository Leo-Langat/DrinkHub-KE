services:
  # ─── Express API Server ───────────────────────────────────────────────────
  - type: web
    name: drinkhub-server
    runtime: node
    region: oregon
    plan: starter
    branch: main
    rootDir: .
    buildCommand: npm install --include=dev && npm run build:server
    startCommand: node apps/server/dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_ACCESS_SECRET
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: JWT_REFRESH_SECRET
        sync: false
      - key: ALLOWED_ORIGINS
        value: "*"

  # ─── React Client (Static Site) ───────────────────────────────────────────
  - type: web
    name: drinkhub-client
    runtime: static
    branch: main
    rootDir: apps/client
    buildCommand: cd ../.. && npm install && npm run build:client
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false
